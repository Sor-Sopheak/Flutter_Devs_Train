import 'package:flutter/material.dart';

class IconButtonCount extends StatelessWidget {
  const IconButtonCount({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("icon with count");
  }
}